{
  "mcpServers": {
    "nishi": {
      "command": "npx",
      "args": ["nishi-mcp-server"],
      "transport": "stdio",
      "env": {
        "NISHI_REPO_PATH": "${workspaceFolder}",
        "NISHI_LOG_LEVEL": "info"
      }
    }
  }
}

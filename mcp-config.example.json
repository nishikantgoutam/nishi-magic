{
  "mcpServers": {
    "devweaver": {
      "command": "npx",
      "args": ["devweaver-mcp-server"],
      "transport": "stdio",
      "env": {
        "DEVWEAVER_REPO_PATH": "${workspaceFolder}",
        "DEVWEAVER_LOG_LEVEL": "info"
      }
    }
  }
}
